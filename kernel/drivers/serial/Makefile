# Makefile fragment for kernel/drivers/serial/.
# Included by kernel/drivers/Makefile - not meant to be run directly,
# it only declares this driver's own source files. The actual compile
# rule and the final link both live in the top-level Makefile, which
# is what turns SRCS_DRIVERS (summed up from every fragment like this
# one) into object files and then into kernel.elf.

SRCS_DRIVERS_SERIAL := \
  kernel/drivers/serial/serial.c

