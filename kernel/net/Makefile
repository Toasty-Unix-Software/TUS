# Makefile fragment for kernel/net/.
# Included by kernel/Makefile - declares this subsystem's own source
# files only. The compile rule and the final link both live in the
# top-level Makefile.

SRCS_NET := \
  kernel/net/dns.c \
  kernel/net/ip.c \
  kernel/net/ipv6.c \
  kernel/net/netif.c \
  kernel/net/socket.c \
  kernel/net/tcp.c \
  kernel/net/udp.c

