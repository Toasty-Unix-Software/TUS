# Makefile fragment for kernel/core/.
# Included by kernel/Makefile - declares this subsystem's own source
# files only. The compile rule and the final link both live in the
# top-level Makefile.

SRCS_CORE := \
  kernel/core/console.c \
  kernel/core/hostname.c \
  kernel/core/klib.c \
  kernel/core/panic_screen.c \
  kernel/core/random.c

