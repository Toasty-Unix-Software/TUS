# Makefile fragment for kernel/vfs/.
# Included by kernel/Makefile - declares this subsystem's own source
# files only. The compile rule and the final link both live in the
# top-level Makefile.

SRCS_VFS := \
  kernel/vfs/devices.c \
  kernel/vfs/procfs.c \
  kernel/vfs/pty.c \
  kernel/vfs/rootfs.c \
  kernel/vfs/vfs.c

